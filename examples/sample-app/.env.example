# Copy this file to `.env` in this folder and fill in your values:
#   cp .env.example .env
# The example loads this local `.env` first, then falls back to a repo-root `.env`.
WRITE_KEY=
DATAPLANE_URL=
